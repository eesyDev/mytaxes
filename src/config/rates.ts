/**
 * Налоговые ставки и константы Казахстан 2026
 *
 * ⚠️ ВАЖНО: С 1 января 2026 года вступил в силу НОВЫЙ Налоговый кодекс РК!
 * Режим Патента отменен, Упрощенка обновлена (лимиты, ставки, отмена СН).
 *
 * Источники:
 * - Закон РК «О республиканском бюджете на 2026–2028 годы»
 * - Новый Налоговый кодекс РК 2026 года
 * - Социальный кодекс РК
 */

export const RATES_2026 = {
  /** Минимальная заработная плата (тенге/мес.) — осталась без изменений */
  MZP: 85_000,

  /** Месячный расчётный показатель (тенге) — новый на 2026 год */
  MRP: 4_325,

  /** СНР на основе упрощенной декларации (Новый НК РК 2026) */
  simplified: {
    /** * Базовая ставка налога: 4%.
     * Примечание: Социальный налог отменен, это чистый ИПН/КПН.
     * Акиматы регионов имеют право корректировать ставку в пределах 2% - 6%.
     */
    taxRate: 0.04,

    /** Лимит дохода теперь считается ЗА ГОД (раньше было за полугодие) */
    incomeMaxMRP: 600_000,

    /** Лимит дохода за год в тенге (2 595 000 000 тг) */
    get incomeMax() {
      return RATES_2026.MRP * this.incomeMaxMRP
    },

    /** Ограничения по количеству наёмных работников ОТСУТСТВУЮТ с 2026 года */
    workersMax: Infinity,

    /** * Старый вычет (уменьшение налога на 1.5% за работника) ОТМЕНЕН.
     * Оставляем 0 для совместимости или чтобы калькулятор не считал старую скидку.
     */
    workersDeductionMax: 0,
  },

  /** * СНР для самозанятых (Введен в 2026 году ВМЕСТО Патента) 
   */
  selfEmployed: {
    /** Единый платеж, заменяющий налоги и соц. отчисления */
    taxRate: 0.04,

    /** Лимит дохода В МЕСЯЦ в МРП */
    incomeMaxMRP_month: 300,

    /** Лимит дохода в месяц в тенге (1 297 500 тг) */
    get incomeMax_month() {
      return RATES_2026.MRP * this.incomeMaxMRP_month
    },
  },

  /** Обязательные пенсионные взносы (ОПВ) */
  opv: {
    rate: 0.10,
    minBaseMZP: 1,
    maxBaseMZP: 50,
  },

  /** * НОВОЕ: Обязательные пенсионные взносы работодателя (ОПВР).
   * С 1 января 2026 года ставка выросла до 3.5%. ИП платят их за себя.
   */
  opvr: {
    rate: 0.035,
    minBaseMZP: 1,
  },

  /** Взносы на ОСМС (ВОСМС) - для ИП за себя */
  vosms: {
    rate: 0.05,
    baseMZP: 1.4, // 5% от 1.4 МЗП
  },

  /** Социальные отчисления (СО) */
  so: {
    rate: 0.05,
    minBaseMZP: 1,
    maxBaseMZP: 7, // Максимальная база для СО — 7 МЗП
  },
}